<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up for Mustache Care Cream</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #e2e2e2, #ffffff);
        }
        .form-container {
            padding: 60px 40px 40px;
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
            max-width: 400px;
            width: 100%;
            text-align: center;
            margin: 20px;
        }
        h2 {
            color: #333;
            margin-bottom: 20px;
        }
        .title {
            font-size: 24px;
            color: #28a745;
            margin-bottom: 20px;
        }
        p {
            color: #555;
            margin-bottom: 20px; /* Margin below paragraph */
        }
        input, button {
            width: 100%;
            padding: 12px;
            margin: 15px 0;
            border: 2px solid #28a745;
            border-radius: 8px;
            transition: border-color 0.3s;
        }
        input:focus {
            border-color: #218838;
            outline: none;
        }
        button {
            background-color: #28a745;
            color: white;
            border: none;
            font-weight: bold;
            transition: background-color 0.3s, transform 0.3s;
        }
        button:hover {
            background-color: #218838;
            transform: translateY(-2px);
        }
        button:active {
            transform: translateY(0);
        }
        label {
            display: block;
            text-align: left;
            margin: 8px 0;
            color: #555;
        }
        .newsletter-label {
            text-align: center;
            margin: 15px 0;
            font-weight: bold;
            color: #28a745;
            text-shadow: 0 0 5px rgba(40, 167, 69, 0.8), 0 0 10px rgba(40, 167, 69, 0.8);
        }
    </style>
</head>
<body>

<div class="form-container">
    <div class="title">Join the Mustache Care Revolution!</div>
    <h2>Sign Up</h2>
    <p>Discover Shiva's favorite mustache care cream for just 99p! This premium blend nourishes and styles your mustache, ensuring you look your best every day. Don't miss out on the chance to elevate your grooming routine!</p>
    <form>
        <label for="name">Name:</label>
        <input type="text" id="name" name="name" required>

        <label for="age">Age:</label>
        <input type="number" id="age" name="age" required>

        <label for="phone">Phone Number:</label>
        <input type="tel" id="phone" name="phone" required>

        <label for="location">Location:</label>
        <input type="text" id="location" name="location" required>

        <label class="newsletter-label">
            <input type="checkbox" name="newsletter" checked>
            Subscribe to Newsletter
        </label>

        <button type="submit">Register</button>
    </form>
</div>

</body>
</html>
